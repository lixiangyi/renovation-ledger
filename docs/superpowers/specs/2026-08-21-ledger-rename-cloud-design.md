# 账本名称云端同步

**日期：** 2026-08-21  
**状态：** 已实现  
**端：** server + Android + 小程序

## 行为

- `PATCH /ledgers/{id}` body `{ "name": "..." }` → 仅**所有者**可改；空名变「新账本」；`revision + 1`。
- 客户端改名：先写本地；已绑云且已登录则 PATCH；成功后用返回名/revision 更新本地（不全量切账本）。
- 未绑云：仅本地。

## 非目标

- 协作者（非 owner）改名权限本期不放开。
